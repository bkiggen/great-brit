# Database connection string
# Local: postgresql://USER@localhost:5432/DATABASE_NAME
# Railway: Will be provided by Railway PostgreSQL service
DATABASE_URL="postgresql://USER@localhost:5432/great_brit"

# Frontend URL for CORS (optional)
# Railway will auto-detect, but you can set this for custom domains
FRONTEND_URL="https://your-frontend-url.com"

# Server port (Railway will set this automatically)
PORT=8000

# Node environment
NODE_ENV="development"
